#ifndef PROJ2_ARRAYFUNCTIONS_H
#define PROJ2_ARRAYFUNCTIONS_H

void bubbleSort(int *array, int size);
int largestValue(int *array, int size); // used for largest in single array
int smallestValue(int *array, int size); // used for smallest in single array
double averageValue(int *array, int size);
int medianValue(int *array, int size); //choose the smaller value if an evenly sized array

#endif
